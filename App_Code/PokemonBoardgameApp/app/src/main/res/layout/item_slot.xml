<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="4dp">

    <!-- EMPTY STATE: centred pokeball -->
    <FrameLayout android:id="@+id/fl_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible">
        <ImageView
            android:layout_width="36dp" android:layout_height="36dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_pokeball"
            android:alpha="0.35"
            android:scaleType="fitCenter"/>
    </FrameLayout>

    <!-- FILLED STATE: sprite / type / name / 2x2 buttons -->
    <LinearLayout android:id="@+id/ll_filled"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="gone">

        <!-- Sprite -->
        <ImageView android:id="@+id/iv_slot_sprite"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="2"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"/>

        <!-- Type badges -->
        <LinearLayout android:id="@+id/ll_slot_types"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingTop="1dp" android:paddingBottom="1dp"/>

        <!-- Name -->
        <TextView android:id="@+id/tv_slot_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="8sp" android:textStyle="bold"
            android:gravity="center" android:maxLines="1"
            android:ellipsize="end"
            android:paddingBottom="2dp"/>

        <!-- 2x2 button grid -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Row 1: BP | Delete -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="2dp">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_slot_bp"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="0dp" android:layout_height="22dp"
                    android:layout_weight="1" android:layout_marginEnd="1dp"
                    android:minWidth="0dp" android:minHeight="0dp" android:padding="0dp"
                    android:text="1" android:textSize="8sp" android:textStyle="bold"
                    android:textAllCaps="false" android:textColor="#333333"
                    android:backgroundTint="#FFFFFF"
                    app:strokeColor="#AAAAAA" app:cornerRadius="4dp"/>

                <ImageButton
                    android:id="@+id/btn_slot_remove"
                    android:layout_width="0dp" android:layout_height="22dp"
                    android:layout_weight="1" android:layout_marginStart="1dp"
                    android:background="@drawable/slot_bg"
                    android:src="@drawable/ic_garbage_bin"
                    android:scaleType="fitCenter"
                    android:padding="3dp"
                    android:contentDescription="Delete"/>
            </LinearLayout>

            <!-- Row 2: Evolve | Duskstone -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <ImageButton
                    android:id="@+id/btn_slot_evolve"
                    android:layout_width="0dp" android:layout_height="22dp"
                    android:layout_weight="1" android:layout_marginEnd="1dp"
                    android:background="@drawable/slot_bg"
                    android:scaleType="fitCenter"
                    android:padding="3dp"
                    android:contentDescription="Evolve"/>

                <ImageButton
                    android:id="@+id/btn_slot_duskstone"
                    android:layout_width="0dp" android:layout_height="22dp"
                    android:layout_weight="1" android:layout_marginStart="1dp"
                    android:background="@drawable/slot_bg"
                    android:scaleType="fitCenter"
                    android:padding="3dp"
                    android:contentDescription="Duskstone"/>
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

</LinearLayout>
