<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp">

    <!-- EMPTY STATE: centred pokeball -->
    <FrameLayout android:id="@+id/fl_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="visible">
        <ImageView
            android:layout_width="36dp" android:layout_height="36dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_pokeball"
            android:alpha="0.35"
            android:scaleType="fitCenter"/>
    </FrameLayout>

    <!-- FILLED STATE: sprite | type-colour bars | name + controls -->
    <LinearLayout android:id="@+id/ll_filled"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:visibility="gone">

        <!-- Sprite -->
        <ImageView android:id="@+id/iv_slot_sprite"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"/>

        <!-- Type icons column: Type1 top-half (aligns with BP row), Type2 bottom-half (aligns with Evolve row) -->
        <LinearLayout android:id="@+id/ll_slot_types"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:orientation="vertical"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"/>

        <!-- Name + controls -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="6"
            android:orientation="vertical">

            <!-- Pokemon Name (DE / EN combined) -->
            <TextView android:id="@+id/tv_slot_name"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="2"
                android:textSize="13sp" android:textStyle="bold"
                android:gravity="center"
                android:maxLines="2"
                android:ellipsize="end"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:background="@drawable/slot_bg"/>

            <!-- 2x2 button grid -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="3"
                android:orientation="vertical"
                android:layout_marginTop="4dp">

                <!-- Row 1: BP | Delete -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="1"
                    android:orientation="horizontal"
                    android:layout_marginBottom="2dp">

                    <!-- BP control: minus | value | plus -->
                    <LinearLayout
                        android:id="@+id/ll_slot_bp"
                        android:layout_width="0dp" android:layout_height="match_parent"
                        android:layout_weight="1" android:layout_marginEnd="1dp"
                        android:orientation="horizontal"
                        android:background="@drawable/slot_bg">
                        <TextView
                            android:id="@+id/btn_slot_bp_minus"
                            android:layout_width="wrap_content" android:layout_height="match_parent"
                            android:text="−" android:textSize="11sp" android:textColor="#CC0000"
                            android:gravity="center" android:paddingHorizontal="4dp"
                            android:includeFontPadding="false"/>
                        <TextView
                            android:id="@+id/tv_slot_bp_value"
                            android:layout_width="0dp" android:layout_height="match_parent"
                            android:layout_weight="1"
                            android:textSize="13sp" android:textStyle="bold"
                            android:textColor="#2E7D32"
                            android:gravity="center"
                            android:includeFontPadding="false"/>
                        <TextView
                            android:id="@+id/btn_slot_bp_plus"
                            android:layout_width="wrap_content" android:layout_height="match_parent"
                            android:text="+" android:textSize="11sp" android:textColor="#CC0000"
                            android:gravity="center" android:paddingHorizontal="4dp"
                            android:includeFontPadding="false"/>
                    </LinearLayout>

                    <ImageButton
                        android:id="@+id/btn_slot_remove"
                        android:layout_width="0dp" android:layout_height="match_parent"
                        android:layout_weight="1" android:layout_marginStart="1dp"
                        android:background="@drawable/slot_bg"
                        android:src="@drawable/ic_garbage_bin"
                        android:scaleType="fitCenter"
                        android:padding="3dp"
                        android:contentDescription="Delete"/>
                </LinearLayout>

                <!-- Row 2: Evolve | Duskstone -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="1"
                    android:orientation="horizontal">

                    <ImageButton
                        android:id="@+id/btn_slot_evolve"
                        android:layout_width="0dp" android:layout_height="match_parent"
                        android:layout_weight="1" android:layout_marginEnd="1dp"
                        android:background="@drawable/slot_bg"
                        android:scaleType="fitCenter"
                        android:padding="3dp"
                        android:contentDescription="Evolve"/>

                    <ImageButton
                        android:id="@+id/btn_slot_duskstone"
                        android:layout_width="0dp" android:layout_height="match_parent"
                        android:layout_weight="1" android:layout_marginStart="1dp"
                        android:background="@drawable/slot_bg"
                        android:scaleType="fitCenter"
                        android:padding="3dp"
                        android:contentDescription="Duskstone"/>
                </LinearLayout>

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
